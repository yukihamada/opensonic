[package]
name = "soluna-sdk"
version = "0.1.0"
edition = "2021"
description = "Soluna OSTP audio relay SDK"
license = "MIT"

[dependencies]
cpal = { version = "0.15", optional = true }
audiopus = { version = "0.3.0-rc.0", optional = true }

[features]
default = []
audio = ["cpal"]
opus = ["audiopus"]
